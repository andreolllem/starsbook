import styled from "styled-components";

export const Container = styled.div`
    width: 100%;
    background: var(--primary);
    padding: 24px;
    margin-top: 100px;

    h1 {
        color: #fff;
    }
`;
